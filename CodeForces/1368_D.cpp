#include <iostream>
#include <vector>

using ull = unsigned long long;

int main() {
    int n, x;
    std::cin >> n >> x;
    std::vector<int> d(n);
    for (int I = 0; I < n; I++) {
        std::cin >> d[I];
    }

    // Traverse two full years sequence in a window of sice x

    // Sum the first x days
    ull ans = 0;


    // Continue traversing the years


}